#include "main.h"

/**
 * mul - multiplies tow integers.
 * @a: the first integer to be multiplied is.
 * @b: the second integer to be multipied is.
 *
 * Return: The result of multiplication.
 */

int mul(int a, int b);
{
	int mul;

	mul = a * b ;

	return (mul);
}
